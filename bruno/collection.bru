headers {
  X-M2M-Key: {{m2mKey}}
}

docs {
  # Deidentify engine

  REST API for pseudonymizing files using the deidentification pipeline.

  ## Base URL
  `http://localhost:8002`

  ## Environment variables
  | Variable     | Description                                                            |
  |--------------|------------------------------------------------------------------------|
  | `baseUrl`    | Base URL of the running server                                         |
  | `outputFile` | Output filename — set automatically after a successful result          |
  | `m2mKey`     | Shared M2M secret; sent as `X-M2M-Key` on every request (gateway mode) |

  ## Typical workflow
  1. **info/app_info** — verify the API is reachable and check settings
  2. **process/submit** — upload an input file, column mapping, and optional datakey
  3. **output/progress** — poll until `percentage` reaches 100
  4. **output/result** — retrieve preview and metrics; output files are written to the shared volume
  5. **process/cancel** — cancel a running process if needed
}
