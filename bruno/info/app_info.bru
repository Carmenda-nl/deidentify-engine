meta {
  name: app_info
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/info
  body: none
  auth: none
}

docs {
  Health check. Returns current server settings:
  `status`, `app_title`, `engine_version`, `host`, `port`, `debug`, `log_level`, `environment`.
}
